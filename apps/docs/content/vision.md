# Vision

AI applications should be built from composable primitives, not monolithic agent frameworks.

## Positioning

This project is not:

- another LangChain clone
- an agent-only framework
- a prompt wrapper

It is:

- a provider-agnostic toolkit
- a tool-first execution standard
- a reusable application infrastructure layer
