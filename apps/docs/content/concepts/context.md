# Context

Context is immutable input to an execution run.

Examples:

- conversation history
- user metadata
- retrieved documents
- application state

Context should be created outside Runtime and passed in.
