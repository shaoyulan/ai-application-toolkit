# GitHub Setup

## Required repository secrets

- `NPM_TOKEN`: npm automation token
- `VERCEL_TOKEN`: optional, only needed if deploying from GitHub Actions manually
- `VERCEL_ORG_ID`: optional
- `VERCEL_PROJECT_ID`: optional

## Branch strategy

- `main`: release branch
- pull requests: CI required
