---
"@ai-application-toolkit/codegraph": patch
---

Make the CLI docs newcomer-friendly: lead with a first-run path (just run `serve` or `index` — no `build` step first), add a commands-at-a-glance table, and clarify that `build` is a throwaway scan, not a setup step.
